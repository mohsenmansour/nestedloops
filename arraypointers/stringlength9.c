#include <stdio.h>
#include <string.h>

int main()
{
    char str[1000] = "GeeksforGeeks";

    // Calling strlen with Str as argument

    printf("Length of the String is %ld \n", strlen(str));

    return 0;
}
